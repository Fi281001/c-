#include<iostream>
using namespace std;

typedef struct date{
	int yyyy;
	int mm;
	int dd;
};

typedef struct book{
	string id;
	string author;
	string title;
	string genre;
	double price;
	date publish;
	string desription;
	
};

void ngay(struct s[]) {
	getline(cin, s.id);
	getline(cin, s.author);
	getline(cin, s.title);
	getline(cin, s.genre);
	cin>>dd>>endl;
	cout<<ngay<<endl;
}
voi xuat(struct v[]){
	
	getline(cin, s.id);
	getline(cin, s.author);
	getline(cin, s.title);
	getline(cin, s.genre);
	cin>>v>>endl;
	cout<<xuat<<endl;
}
int main() {
	
	return 0;
}
